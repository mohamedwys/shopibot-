// Supported locales — must match your folder names
export type SupportedLocale = "en" | "es" | "fr" | "de" | "ja" | "it" | "pt" | "zh";

// Default namespace — you can add more namespaces if needed later
export type DefaultNamespace = "common"; 
